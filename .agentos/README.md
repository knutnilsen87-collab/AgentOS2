# .agentos runtime data

This folder stores local runtime artifacts for phase-1 development.

- `tasks/` — saved task and run JSON files
- `memory/` — lightweight local memory artifacts
- `artifacts/` — generated local outputs for review
- `cache/` — ephemeral caches

Keep only the `.gitkeep` files committed by default.
